# This program just prints out an extra question for this week - lab 3.1
# Author Ellen McGrory

message = 'I have eaten ' + str(99) + ' burritos.' 
print (message)

# Eggs is a valid variable name as it starts with a letter which is the Pythonic way of programming, while starting a 
# variable with 100 is not Pythonic as a letter should be used at the start. However the number can be used later in the 
# variable name after a letter than being used. Starting a variable with a letter is the correct python syntax

# The int(), float(), and str() functions